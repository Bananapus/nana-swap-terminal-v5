// SPDX-License-Identifier: MIT
pragma solidity ^0.8.17;

import "../helper/UnitFixture.sol";

import {MockERC20} from "forge-std/mocks/MockERC20.sol";


/// @notice test pay(..)
contract EmptyTest_Unit is UnitFixture {

    address tokenIn;
    address sender;

    function setUp() public override {
        super.setUp();

        sender = makeAddr("sender");
    }

    modifier nativeTokenIn() {
        tokenIn = JBConstants.NATIVE_TOKEN;
        _;
    }

    modifier erc20TokenIn(uint256 amount) {
        tokenIn = makeAddr("erc20in");
        _;
    }

    function testOne(uint256 amountIn) public erc20TokenIn(amountIn) {
        assertEq(true, true);
    }


}
